# 大模型接口配置（OpenAI 兼容格式）
#
# ── 怎么用 ────────────────────────────────────────────────
# 复制本文件为 .env，填入真实值，然后 npm run dev（或 npm run build）。
# 不填也能跑 —— 结果页会走内置文案，图表与百分位完全不受影响。
# 「没配密钥」是一种正常状态，不是错误状态。
#
# ── 一句必须说的话 ───────────────────────────────────────
# VITE_ 前缀的变量会被打进浏览器 bundle，任何打开开发者工具的人都拿得到。
# 所以：
#   · 本地自己看效果 —— 可以填；
#   · 部署到静态站点 —— **不要填**。公开的密钥等于没有密钥。
# 线上那一版刻意走内置文案；真实流式输出的效果在演示视频里用本地 .env 跑。
# 真要在线上用，得配一个自己的后端做转发，那就不是纯前端项目了。
#
# .env 已被 .gitignore 排除，不会进仓库。

VITE_LLM_BASE_URL=https://api.example.com/v1
VITE_LLM_API_KEY=sk-在这里填你的密钥
VITE_LLM_MODEL=gpt-4o-mini
