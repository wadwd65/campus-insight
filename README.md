# 你的大学平行宇宙

> 2026 第九届传智杯全国大学生数智创新与 AI 应用大赛 · AI创新应用挑战赛
> 个人赛 · B 组

**选题方向**：方向 6（校园 / 垂直社区全栈 MVP，数据看板类）
**产品形态**：个人青春行为图谱生成器 —— 答 10 题，约 1 分钟生成一份专属的「大学时代数据叙事报告」

> **两条玩法，同一本账**：问卷是**一次性**的（答完出报告）；**行动地图**是可反复的
> （4 个时段、16 个地点，每次行动都改写八维属性，**不答题也能出档案**）。

---

## 这是什么

```
答 10 道有画面感的选择题（约 1 分钟）
        ↓
8 个潜在属性的连续值 × 与 2000 人基准人群比对
        ↓
五件套报告：雷达图 · 时间分配环形图 · 四年心情曲线 · 人群冷知识 · 大模型个性化总结

或者走地图：4 个时段 / 16 个地点 / 同一套计算 → 同样出这份报告
```

**它和你见过的所有「数据看板」都不一样的地方在于**：

普通看板让你**看别人的数据**。这个产品让你**成为数据源** —— 不需要账号、不需要上传、不需要安装任何东西，只需要回答几个有共鸣的简单问题，然后看到一份属于**你自己**的报告。

人类对自己的好奇心，远大于对抽象数据的好奇。这是本产品唯一依赖的机制，也是它最难被复制的地方。

**示例输出**（真的跑出来的，不是设计稿）：

> 在「学术投入」上，你超过了 98% 的大学生 —— 你的书桌像有结界，坐下就能学进去
> 你的称号：**图书馆常驻民**
> 四年曲线：大一 59 → 大二 66 → 大三 71 → 大四 65（「别人开始慌的时候，你在按表推进」）

---

## 官方要求与本产品的对应

官方对方向 6 的原始描述：

> 数据源（CSV / JSON 或公开 API）→ ECharts / D3 动态图表大盘 → 大模型对长尾数据做**趣味**解读

| 官方要求 | 本产品对应 |
|---|---|
| 数据源（CSV） | `public/data/问卷基准数据.csv`（2000 条模拟问卷）；真入口另可上传自己的 CSV |
| 动态图表大盘 | ECharts 雷达图 / 环形图 / 折线图，均带入场动画 |
| 长尾数据 | 8 个潜在属性的分布，以及「你排在人群第几」的百分位 |
| **趣味**解读 | 大模型生成带称呼的个性化总结 + 称号 + 冷知识句 |

## 功能清单

完整任务表（含每项的产出物与验收标准）见 **`docs/开发任务表-大学平行宇宙.md`**。

| 入口 | 做什么 | 状态 |
|---|---|---|
| 快入口（问卷） | 答 10 题 → 约 1 分钟出个人报告（评委现场玩） | ✅ 已跑通：雷达图 / 时间分配 / 四年曲线 / 冷知识 / 大模型解读，五件套齐全 |
| 行动地图（游戏层） | 4 个时段 / 16 个地点，反复走、增量衰减；**不答题也能出档案** | ✅ 已跑通：四区地图 / 属性实时反馈 / 出行轨迹 / 报告多出「你走过的路」 |
| 真入口 | 上传批量问卷 CSV → 出群体画像（教师看班级） | ✅ 已跑通：亚型分布 / 属性长短对比 / 与你最像的一类人；附 48 人示例数据可直接试 |

## 核心设计

产品最要紧的技术含量在**映射矩阵**：10 个问题、48 个选项，为什么能撑起四个维度各异的输出？

答案是插一层 **8 个潜在属性**作为中间层 —— 每个选项同时影响多个属性，而四套派生规则各自从属性出发。于是「选项数少、输出维度多」这个矛盾被解开，而两个人只要有一题不同，8 维向量就会分叉，分叉会同时传导到所有输出上。

完整设计（含 8 个属性的定义、48 个选项的增量矩阵、四套派生公式、两道自动闸门）见 **`docs/设计-01-问题与映射矩阵.md`**。

**「结果因人而异」不是一句口号，是有闸门守着的**：`npm run check` 会随机生成 5000 份作答，量化检验输出分化度。实测雷达图两两平均距离 70.0（门槛 18），200 对抽样中重复 0 对；降级文案（没配密钥时用的那份）在 5000 份随机作答下，开头段写出了 1584 种不同版本。

这两道闸门守的其实是同一件事：**一个产品如果不同的人用完看起来都差不多，那它最值钱的那个瞬间——「这就是我」——根本不会发生。** 而这件事人眼验证不了（你不可能手点 5000 次），只能量化。

## 10 分钟极速复现指南

> 评审老师按这一节操作，十分钟内可看到完整效果。
> 三条路都能直接体验：**快入口**答 10 题即出个人报告；**行动地图**走 4 步不答题也出报告；**真入口**上传 `public/data/示例-班级问卷.csv` 即出群体画像。
> ⚠️ 本节目前是开发期版本，**最终版会在干净环境完整实测后再定稿**（届时与线上部署链接一并给出）。

### 环境要求

| 项 | 版本 |
|---|---|
| Node.js | 20.19 以上（开发环境为 22.22.2） |
| npm | 10 以上 |
| 浏览器 | Chrome / Edge 最新版 |

### 步骤

```bash
# 1. 获取代码
git clone https://github.com/wadwd65/campus-insight.git
cd campus-insight

# 2. 安装依赖
npm install

# 3. 配置环境变量（可选，不配也能看完整报告）
cp .env.example .env
# 打开 .env，填入大模型接口地址与密钥（见下表）

# 4. 启动
npm run dev

# 5. 打开终端里显示的地址（默认 http://127.0.0.1:5173）

# 6.（可选）跑一遍全部自检，看「结果因人而异」的量化证据
npm run check     # 379 项：124 计算层 + 39 终端账本 + 54 地图 + 89 入场推镜 + 73 页面渲染冒烟
```

### 环境变量

| 变量 | 说明 | 示例 |
|---|---|---|
| `VITE_LLM_BASE_URL` | 大模型接口地址（OpenAI 兼容格式） | `https://api.example.com/v1` |
| `VITE_LLM_API_KEY` | 接口密钥 | `sk-xxxxxxxx` |
| `VITE_LLM_MODEL` | 使用的模型名 | `gpt-4o-mini` |

### 没有密钥也能看

大模型不是承重结构：未配置密钥或接口不可用时，页面切换到**内置文案模板**，图表与百分位保持完整 —— 「自己答完看到自己的结果」这个核心机制不依赖任何外部服务。

## 技术栈

| 层 | 选型 | 版本 |
|---|---|---|
| 构建 | Vite | 8.3.0 |
| 界面 | React | 19.3.0 |
| 样式 | Tailwind CSS | 4.3.3 |
| 图表 | ECharts | 6.1.0（按需注册，结果页单独分包） |
| 表格解析 | PapaParse | 5.7.0 |
| 大模型 | OpenAI 兼容接口 + SSE 流式输出 | 不引第三方 SDK，直接调 `fetch` |

无数据库、无后端服务；可部署为纯静态站点。

## 目录结构

```
campus-insight/
├── src/
│   ├── components/   界面组件（只画，不算）
│   ├── lib/          纯计算逻辑（不 import React，可单独验证）
│   ├── data/         数据接入（matrix.json 与取数代码）
│   ├── App.jsx
│   ├── main.jsx
│   └── index.css
├── public/
│   └── data/         数据文件（随代码提交）
├── scripts/          数据生成与自检脚本
├── docs/             选型、设计、留痕规范、开发任务表
├── prompts/          与 AI 的对话记录（随代码提交）
├── index.html
├── vite.config.js
└── package.json
```

## 数据声明

⚠️ `public/data/问卷基准数据.csv` 为**模拟生成**，不是真实调查结果，不含任何真实个人信息。

它由 `scripts/generate-survey-data.py` 按固定的随机种子生成，可完整复现（内含 6 种设定好的人群亚型，用于让属性分布具备长尾特征 —— 否则「你超过了 88% 的人」这句话会失去意义）。

## 开发过程

| 材料 | 位置 |
|---|---|
| 开发任务表（功能清单与验收标准） | `docs/开发任务表-大学平行宇宙.md` |
| **设计：10 题与映射矩阵** | `docs/设计-01-问题与映射矩阵.md` |
| 方案定案（含 D-1~D-6 拍板记录） | `docs/选型-05-大学平行宇宙-方案定义.md` |
| 市面形态调研（为什么不做普通看板） | `docs/选型-04-市面形态调研.md` |
| **方向推翻与重选记录** | `docs/选型-03-方向反思与重选.md` |
| 方向 4 与方向 6 对比 | `docs/选型-01-方向4与方向6对比.md` |
| 留痕规范（三件套） | `docs/留痕规范.md` |
| 与 AI 的完整对话记录 | `prompts/` |

> 「方向推翻与重选」这一条不是走过场：本项目最初定的方案（学习投入诊断看板）在实现到一半时被主动推翻 —— 理由是形态是标准答案、结论不反直觉、没有留人机制。推翻发生在图表层开工之前，因此代价最低；已完成的解析 / 校验 / 清洗 / 自检框架被完整继承。整个过程留在 Git 历史与 `docs/选型-03` 里。

## 许可

待定
